# Dockerfile

# Step 1: Use the official Nginx image from Docker Hub as the base image
FROM nginx:alpine

# Step 2: Copy the HTML file into the Nginx container
COPY index.html /usr/share/nginx/html/index.html

# Step 3: Expose port 80
EXPOSE 80
